import React from 'react';

class Content extends React.Component {
    constructor(props) {
        super(props)
    }

    render() {
        // here we need to render 'My Library' by default and re-render when user
        // clicks something else or filters items

        return(
            <div className='content'>

            </div>
        )
    }
}